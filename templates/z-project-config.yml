# ZebraCode Project Configuration
# Generated by /z-project-init
# Location: {project-root}/.claude/z-project-config.yml

project:
  name: ""                            # Project name
  description: ""                     # Brief description

git:
  target_branch: "main"               # Branch to merge into ("main" or "master")
  branch_prefix: ""                   # e.g., "feature/", "fix/" — empty = issue ID only

issue_tracker:
  type: "github"                      # "jira" | "github" | "linear" | "none"
  project_key: ""                     # Jira project key / GitHub repo
  mcp_tool: null                      # Exact MCP tool name for fetching issues

stack:
  language: ""                        # Primary language (e.g., "php", "typescript", "python")
  framework: ""                       # Primary framework (e.g., "laravel", "nextjs", "django")
  frontend: null                      # Frontend framework (e.g., "react", "vue", "jquery")
  css: null                           # CSS framework (e.g., "tailwind", "bootstrap4")
  test_runner_backend: null           # Backend test runner (e.g., "phpunit", "jest", "pytest")
  test_runner_frontend: null          # Frontend test runner (if any)
  type_checker: null                  # "tsc" | "phpstan" | null
  formatter: null                     # "prettier" | "eslint" | "pint" | null
  linter: null                        # "eslint" | "phpstan" | "phpcs" | null

context7_ids:                         # Pre-resolved Context7 library IDs (skip resolve-library-id calls)
  # framework: "/laravel/laravel"
  # frontend: "/vuejs/core"
  # css: "/tailwindlabs/tailwindcss"
  # test_runner: "/sebastianbergmann/phpunit"

commands:
  test_backend: null                  # e.g., "./vendor/bin/sail test", "npm test"
  test_frontend: null
  lint: null                          # e.g., "npm run lint"
  format: null                        # e.g., "npx prettier --write"
  typecheck: null                     # e.g., "npx tsc --noEmit"
  build: null                         # e.g., "npm run build"
  dev_server: null                    # e.g., "npm run dev"
  continuous_dev: false               # If true, user runs dev server externally
  migrate: null                       # e.g., "./vendor/bin/sail artisan migrate"
  clear_caches: null                  # e.g., "./vendor/bin/sail artisan cache:clear"

dangerous_commands:                   # Blocked by destructive command hook
  # Add project-specific dangerous commands here
  # seed_fresh: null

protected_files:                      # Patterns blocked by file protection hook
  - ".env*"
  - "package-lock.json"
  - "composer.lock"
  - "yarn.lock"
  - "pnpm-lock.yaml"
  # Add project-specific patterns here

docs:
  main_doc: "DOCS.md"                # Primary documentation file
  claude_md: "CLAUDE.md"             # Project CLAUDE.md location
